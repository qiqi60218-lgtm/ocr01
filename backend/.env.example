# LLM（如豆包/OpenAI兼容）通用配置示例
# 将此文件复制为 .env 并填入你的真实配置

# 提供商（可选：doubao、openai、custom）
LLM_PROVIDER=doubao

# API基础地址（示例）
# Doubao/火山引擎网关示例（请替换为你的网关地址）：
# LLM_API_BASE=https://api.doubao.example
# OpenAI兼容示例：
# LLM_API_BASE=https://api.openai.example
LLM_API_BASE=

# API密钥（建议放在环境变量或安全管理系统中）
LLM_API_KEY=

# 模型名称（示例）
# Doubao示例：doubao-pro-128k / doubao-lite / 等
# OpenAI兼容示例：gpt-4o-mini / gpt-4o / 等
LLM_MODEL=

# 可选：自定义端点（优先于默认 /v1/chat/completions）
LLM_ENDPOINT=

# 采样温度与超时
LLM_TEMPERATURE=0.2
LLM_TIMEOUT=30

# 远程OCR（豆包图像识别）配置示例
REMOTE_OCR_API_BASE=
REMOTE_OCR_API_KEY=
REMOTE_OCR_MODEL=
# 如你的服务端点不是默认 /v1/ocr，请明确写出
REMOTE_OCR_ENDPOINT=
REMOTE_OCR_TIMEOUT=30